# Base URL of the monitor-service API.
# Local dev: leave as the default below (or omit the file entirely).
# On Vercel: set VITE_API_URL to your deployed API URL (e.g. https://<app>.fly.dev).
VITE_API_URL=http://localhost:3000
