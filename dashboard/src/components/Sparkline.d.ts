import type { HistoryPoint } from '../types';
interface Props {
    history: HistoryPoint[];
    color: string;
}
export default function Sparkline({ history, color }: Props): import("react").JSX.Element;
export {};
